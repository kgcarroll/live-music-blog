/** Ticketmaster schedule feed: page size and cache TTL. */
export const SCHEDULE_PAGE_SIZE = 9

/** Greater Philadelphia DMA (Ticketmaster Designated Market Area). Override with TICKETMASTER_DMA_ID. */
export const SCHEDULE_DMA_ID_DEFAULT = '358'

/** Upcoming window for Discovery API date filters. */
export const SCHEDULE_DAYS_AHEAD = 30

/** ISR / fetch revalidate for Ticketmaster responses (seconds). */
export const SCHEDULE_REVALIDATE_SECONDS = 3600
