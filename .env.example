# Sanity (public reads + Studio)
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
# Viewer token - required for draft mode + live preview (sanity.io/manage, API, Tokens)
SANITY_API_READ_TOKEN=

# Site URL for Presentation tool iframe + stega (production: https://your-domain.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: full Studio URL if Studio is hosted elsewhere (defaults to NEXT_PUBLIC_SITE_URL + /studio)
# NEXT_PUBLIC_SANITY_STUDIO_URL=http://localhost:3000/studio

# Contact form (Resend — https://resend.com)
# 1. Create an API key. 2. Set CONTACT_TO_EMAIL to the inbox that should receive messages.
# 3. Without a verified domain, leave RESEND_FROM unset (uses onboarding@resend.dev) and ensure
#    CONTACT_TO_EMAIL is the same address as your Resend account (testing restriction).
# 4. For production, verify your domain in Resend and set RESEND_FROM to e.g. "Live Music Blog <hello@yourdomain.com>"
RESEND_API_KEY=
CONTACT_TO_EMAIL=
# RESEND_FROM=Live Music Blog <hello@yourdomain.com>

# Newsletter (footer signup + Sanity newsletterIssue + Resend Audiences/Broadcasts)
# After first deploy, set RESEND_AUDIENCE_ID from server logs if the app auto-created an audience.
# RESEND_AUDIENCE_ID=
# RESEND_AUDIENCE_NAME=Philadelphia Music Live Newsletter
# Secret for POST /api/newsletter/send (Studio Send / Send test). Set the same value for Studio:
NEXT_PUBLIC_NEWSLETTER_SEND_SECRET=
NEWSLETTER_SEND_SECRET=
# Optional: separate secret for confirm links (defaults to NEWSLETTER_SEND_SECRET)
# NEWSLETTER_CONFIRM_SECRET=
# Required to save sentAt on the issue after broadcast (Sanity API token with Editor write access)
# SANITY_API_WRITE_TOKEN=

# Optional: Sanity webhook POST /api/revalidate (instant ISR)
SANITY_REVALIDATE_SECRET=

# Ticketmaster Discovery API (/events) — server-only Consumer Key from developer.ticketmaster.com
TICKETMASTER_API_KEY=
# Optional: override DMA for metro filter (default 358 = greater Philadelphia)
# TICKETMASTER_DMA_ID=358

# Mapbox public token for /venues map (browser — restrict by URL in Mapbox dashboard)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Algolia sitewide search (/search) — use a search-only API key for the public vars.
# This app reads the names below first; the Sanity Algolia guide’s names work as fallbacks.
# Guide: https://www.sanity.io/docs/developer-guides/how-to-implement-front-end-search-with-sanity
#   Guide → this repo: NEXT_PUBLIC_ALGOLIA_APP_ID → same as APPLICATION_ID
#   Guide → this repo: NEXT_PUBLIC_ALGOLIA_API_KEY → same as SEARCH_API_KEY
#   Guide → this repo: ALGOLIA_WRITE_KEY → same as ALGOLIA_ADMIN_API_KEY (reindex / CLI only)
NEXT_PUBLIC_ALGOLIA_APPLICATION_ID=
NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY=
NEXT_PUBLIC_ALGOLIA_INDEX_NAME=
# Admin key: never expose to the browser. Used for `npm run algolia:index` and POST /api/algolia/reindex.
ALGOLIA_ADMIN_API_KEY=
# Secret for POST /api/algolia/reindex. Not required for local CLI index.
# Sanity GROQ webhook: put the same string in the webhook “Secret” field in manage.sanity.io — no custom headers needed
# (Sanity sends `sanity-webhook-signature`; this app verifies it with next-sanity/webhook).
# Or use curl / other clients: `Authorization: Bearer <ALGOLIA_REINDEX_SECRET>` or `?secret=`.
ALGOLIA_REINDEX_SECRET=
# Optional: only if your provider sends a *custom* header name (must be a valid HTTP token, e.g. X-Algolia-Reindex-Secret).
# This env var’s *value* is that header name. Do not type ALGOLIA_REINDEX_SECRET_HEADER into Sanity as the header key.
# ALGOLIA_REINDEX_SECRET_HEADER=X-Algolia-Reindex-Secret
